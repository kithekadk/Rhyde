import { HostbinderDirective } from './hostbinder.directive';

describe('HostbinderDirective', () => {
  it('should create an instance', () => {
    const directive = new HostbinderDirective();
    expect(directive).toBeTruthy();
  });
});
